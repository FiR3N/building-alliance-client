import { IMixture } from "../Entity/IMixture";
export interface IMixtureForm {
  mixtureType: string;
  mixture: string;
  count: number;
  price: number;
  name: string;
  phone: string;
  email: string;
  text: string;
  address: string;
}
